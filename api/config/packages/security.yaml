security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    # Provider basé sur l'entité User (identifiant = email).
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false

        # Connexion : reçoit email + mot de passe en JSON, renvoie un token JWT.
        login:
            pattern: ^/api/login$
            stateless: true
            json_login:
                check_path: /api/login
                username_path: email
                password_path: password
                success_handler: lexik_jwt_authentication.handler.authentication_success
                failure_handler: lexik_jwt_authentication.handler.authentication_failure

        # API : sans session, protégée par le token JWT.
        api:
            pattern: ^/api
            stateless: true
            provider: app_user_provider
            jwt: ~

    # Première règle qui correspond = appliquée.
    access_control:
        - { path: ^/api/login, roles: PUBLIC_ACCESS }
        # Réservation publique (site client) : accessible sans authentification.
        - { path: ^/api/public, roles: PUBLIC_ACCESS }
        # Documentation de l'API (Swagger UI + OpenAPI JSON) : accessible sans authentification.
        - { path: ^/api/doc, roles: PUBLIC_ACCESS }
        # Tout le reste de l'API nécessite d'être connecté.
        - { path: ^/api, roles: IS_AUTHENTICATED_FULLY }

when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
